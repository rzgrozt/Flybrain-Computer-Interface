# Project scripts

Keep thin command-line entry points here only when they are unsuitable for package
entry points. Domain logic belongs in `src/flybrain_interface/` so it remains
testable and reusable.
