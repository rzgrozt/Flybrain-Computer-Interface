project:
  name: flybrain-computer-interface
  seed: 0

data:
  manifest: data/manifests/malecns-v1.0.json
  raw_directory: data/raw
  processed_directory: data/processed

connectome:
  dataset_directory: data/processed/malecns-v1.0
  sign_policy: inhibitory-glutamate-modulators-separated-v1
  ground_truth_overrides_consensus: true

runtime:
  deterministic: true
  device: cpu

simulation:
  model: shiu-lif
  resting_mv: -52.0
  reset_mv: -52.0
  threshold_mv: -45.0
  membrane_tau_ms: 20.0
  synapse_tau_ms: 5.0
  refractory_ms: 2.2
  delay_ms: 1.8
  synapse_scale_mv: 0.275

clocks:
  neural_dt_ms: 0.1
  visual_hz: 30.0
  motor_hz: 25.0
  reward_hz: 20.0
  telemetry_hz: 10.0
  optional_ai_hz: 1.0

optional_ai:
  enabled: false
  semantic_provider: null
  visual_provider: null
